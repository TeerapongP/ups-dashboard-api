# ระบบตรวจจับไฟตก (Power Failure Detection)

## 📋 ภาพรวม

ระบบตรวจจับไฟตกได้รับการปรับปรุงให้ละเอียดและแม่นยำขึ้น โดยสามารถตรวจจับสถานการณ์ไฟตกได้หลากหลายรูปแบบ

## 🔍 เกณฑ์การตรวจจับ

### 1. **Online** (ปกติ)
- Input voltage ทุกเฟส ≥ 180V
- Output voltage > 0
- ระบบไฟฟ้าทำงานปกติ

### 2. **Powerfail** (ไฟตก) ⚠️
เกิดขึ้นเมื่อมีเงื่อนไขใดเงื่อนไขหนึ่งต่อไปนี้:

#### เงื่อนไข A: ไฟดับสนิท
- Input voltage ทุกเฟส = 0V
- Output voltage > 0 (UPS ยังจ่ายไฟจากแบตเตอรี่)

#### เงื่อนไข B: ไฟตกบางส่วน (ใหม่!)
- Input voltage เฟสใดเฟสหนึ่ง: **0 < voltage < 180V**
- Output voltage > 0
- ตรวจจับได้ทั้ง L1, L2, L3

### 3. **Offline** (ปิด)
- Output voltage ทุกเฟส = 0V
- UPS ปิดสนิท หรือไม่สามารถจ่ายไฟได้

## 📊 ตัวอย่างสถานการณ์

| สถานการณ์ | Input L1 | Input L2 | Input L3 | Output | สถานะ | คำอธิบาย |
|-----------|----------|----------|----------|---------|--------|----------|
| ไฟปกติ | 220V | 220V | 220V | 220V | **Online** | ทำงานปกติ |
| ไฟดับสนิท | 0V | 0V | 0V | 220V | **Powerfail** | ใช้แบตเตอรี่ |
| ไฟตก L1 | 150V | 220V | 220V | 220V | **Powerfail** | L1 ต่ำกว่า 180V |
| ไฟตกหลายเฟส | 160V | 170V | 220V | 220V | **Powerfail** | L1,L2 ต่ำกว่า 180V |
| ไฟต่ำเล็กน้อย | 190V | 195V | 200V | 220V | **Online** | ยังไม่ถึงเกณฑ์ |
| UPS ปิด | 0V | 0V | 0V | 0V | **Offline** | ไม่มีเอาต์พุต |

## 🔧 การใช้งาน

### API Endpoints

#### ตรวจสอบเครื่องเดียว
```bash
GET /api/ups/{ip}?persist=true
```

#### ตรวจสอบทุกเครื่อง
```bash
GET /api/ups-getall?persist=true
```

### ตัวอย่าง Response เมื่อไฟตก

```json
{
  "id": "UPS_10_40_1_10",
  "ip": "10.40.1.10",
  "status": "Powerfail",
  "brand": "Smart Power",
  "location": "แฟลตบุคลากร 1",
  "input": {
    "L1V": 150,  // ← ต่ำกว่า 180V
    "L2V": 220,
    "L3V": 220
  },
  "output": {
    "L1V": 220,  // ← ยังจ่ายได้ปกติ
    "L2V": 220,
    "L3V": 220
  },
  "batteryPercent": 85,
  "temperatureC": 28,
  "collected_at": 1703123456.789
}
```

## 📝 Event Logging

เมื่อตรวจพบไฟตก ระบบจะบันทึก event ลงฐานข้อมูลพร้อมรายละเอียด:

### ข้อมูล Event
- **Event Type**: `powerfail`
- **Severity**: `warning`
- **Message**: รายละเอียดสาเหตุ เช่น "Power failure detected - L1: 150V (ไฟตก), L2: 0V (ไฟดับ)"
- **Event Data**: ค่าแรงดันแต่ละเฟสและสาเหตุ

### ตัวอย่าง Event Data
```json
{
  "input_voltages": {
    "L1V": 150.0,
    "L2V": 0.0,
    "L3V": 220.0
  },
  "detection_reason": "voltage_below_threshold"
}
```

## ⚙️ การกำหนดค่า

### Voltage Thresholds (ใน `app/constants.py`)
```python
class VoltageThresholds:
    POWER_FAIL = 180.0      # เกณฑ์ไฟตก
    NOMINAL_MIN = 200.0     # แรงดันปกติขั้นต่ำ
    NOMINAL_MAX = 250.0     # แรงดันปกติสูงสุด
    CRITICAL_LOW = 160.0    # แรงดันต่ำมาก
    WARNING_LOW = 190.0     # แรงดันต่ำ (เตือน)
```

## 🔄 การทำงานของระบบ

1. **การเก็บข้อมูล**: ระบบดึงข้อมูล SNMP จาก UPS ทุก 2-5 วินาที
2. **การประเมินสถานะ**: ฟังก์ชัน `_human_status()` วิเคราะห์ค่าแรงดัน
3. **การบันทึก Event**: เมื่อสถานะเปลี่ยน จะบันทึก event ลงฐานข้อมูล
4. **การแจ้งเตือน**: สามารถต่อยอดเป็นระบบแจ้งเตือนได้

## 🚀 ข้อดีของระบบใหม่

1. **ตรวจจับได้ละเอียดขึ้น**: รองรับไฟตกบางส่วน (partial power failure)
2. **ข้อมูลครบถ้วน**: บันทึกสาเหตุและค่าแรงดันแต่ละเฟส
3. **ปรับแต่งได้**: สามารถเปลี่ยนเกณฑ์ได้ง่าย
4. **Event Timeline**: ติดตามประวัติการเกิดเหตุการณ์
5. **API Ready**: พร้อมใช้งานผ่าน REST API

## 🔧 การทดสอบ

รันไฟล์ทดสอบ:
```bash
python test_power_failure_detection.py
```

จะแสดงผลการทดสอบสถานการณ์ต่างๆ และเกณฑ์การตรวจจับ

---

**หมายเหตุ**: ระบบนี้ออกแบบมาเพื่อความปลอดภัยและความน่าเชื่อถือ หากมีข้อสงสัยหรือต้องการปรับแต่งเพิ่มเติม สามารถแก้ไขค่าใน `app/constants.py` ได้